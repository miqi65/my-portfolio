"use client";

import ProjectNextSection from "@/components/ProjectNextSection";

export function CaseNextProject({ currentSlug }: { currentSlug: string }) {
  return <ProjectNextSection currentSlug={currentSlug} />;
}
